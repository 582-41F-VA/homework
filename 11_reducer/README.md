# Homework: Reducer

For this homework, your task is to implement a `ShoppingCart` component
that displays a given list of products, each with a name, ID, quantity
and unit price. The total price for a product (unit × quantity) and the
overall order total should also be shown. Users can remove products,
adjust their quantity, and empty the cart. These actions are handled
using a reducer.
