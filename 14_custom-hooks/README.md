# Homework: Custom hooks

Add a `useLocation` custom hook to the client-side router built in
class. When called, the hook should return an object with a `pathname`
parameter whose value is the current path.
